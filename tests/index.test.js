
describe("", () => {
    const desc = "<p>Check sum of numbers</p>";

    describe(desc, () => {
        it("Should return 595", () => {
            const result = sum
            expect(result).toBe(595)
        });
    });
});

describe("", () => {
    const desc = "<p>Find min and max</p>";

    describe(desc, () => {
        it("Should return 10 and 109", () => {
            const resultmin = min
            const resultmax = max
            expect(resultmin).toBe(10)
            expect(resultmax).toBe(109)
        });
    });
});

describe("LESSON 23", () => {
    const desc = "<p>Build tree</p>";

    describe(desc, () => {
        it("Should return tree", () => {
            const resulttree = tree
            expect(resulttree).toBe(tree += "#")
        });
    });
});

describe("", () => {
    const desc = "<p>Accumulator</p>";

    describe(desc, () => {
        it("Should return tree", () => {
            const resulttree = tree
            expect(resulttree).toBe(tree += "#")
        });
    });
});


